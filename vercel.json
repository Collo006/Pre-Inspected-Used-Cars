{
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && npx prisma generate"
      }
    }
  ],
  "rootDirectory": "client"
}